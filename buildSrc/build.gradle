apply plugin: 'groovy'


sourceSets {
    main {
        groovy {
            srcDir 'src/main/groovy'
        }

        resources {
            srcDir 'src/main/resources'
        }
    }

}
dependencies {
    implementation gradleApi()
}
repositories {
    mavenCentral()
}
